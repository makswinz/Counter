namespace FocusNotch.Core.Models;

/// <summary>What the user picked in the tray or the settings surface.</summary>
public enum ThemePreference
{
    /// <summary>Follow the Windows app theme. The first-run default.</summary>
    System = 0,
    Light = 1,
    Dark = 2
}
